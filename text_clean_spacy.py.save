import pandas as pd
import numpy as np
import regex as re

import sys

SYMBOL_FILTER = re.compile("[!@#%:;,\.?\'\"]")
CLEAN_UP = re.compile("[^a-z]")
WHITESPCAE_FILTER = re.compile("  *")

def extract_words(x):
    x = " ".join(WHITESPCAE_FILTER.sub(" "," ".join(CLEAN_UP.sub(" "," 
".join(SYMBOL_FILTER.sub("",x.lower()).split())).split())).split()) df = 
pd.read_csv(f"./{sys.argv[1]}") comments = df.comments_text.apply()
    return x
